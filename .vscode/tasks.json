{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Web Dev Server",
            "type": "shell",
            "command": "uv run uvicorn main:app --reload"
        },
        {
            "label": "Run Desktop App",
            "type": "shell",
            "command": "uv run desktop.py"
        },
        {
            "label": "Run Tests",
            "type": "shell",
            "command": "uv run pytest tests/ -v"
        }
    ]
}
